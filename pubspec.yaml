name: touch365_scanner
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  simple_barcode_scanner: ^0.4.0
  file_picker: ^10.3.8
  path_provider: ^2.1.5
  intl: ^0.20.2
  flutter_spinbox: ^0.13.1
  flutter_file_dialog: ^3.0.3
  google_mlkit_barcode_scanning: ^0.12.1
  mailer: ^6.6.0
  supabase_flutter: ^2.12.0
  flutter_secure_storage: ^10.0.0
  share_plus: ^12.0.1
  shared_preferences: ^2.5.4
  sqflite: ^2.4.2
  mobile_scanner: ^7.2.0 
  in_app_purchase: ^3.2.3
  flutter_dotenv: ^6.0.0
  camera: ^0.12.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: 0.14.4

flutter:
  uses-material-design: true
  assets:
    - lib/secret.json
    - .env

flutter_icons:
  android: true
  ios: false
  image_path: assets\images\Scanner365.png




